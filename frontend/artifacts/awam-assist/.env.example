# URL of the deployed Express backend (no trailing slash).
# Example: https://awam-assist-api.onrender.com
# Leave empty when running on Replit (the proxy handles routing automatically).
VITE_API_BASE_URL=
