/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import * as zod from "zod";

/**
 * Returns server health status
 * @summary Health check
 */
export const HealthCheckResponse = zod.object({
  status: zod.string(),
});

/**
 * Sends the user's chat history (with the latest user message) and a category filter, returns the assistant's reply.
 * @summary Send a chat message and get a reply
 */
export const SendChatMessageBody = zod.object({
  messages: zod
    .array(
      zod.object({
        role: zod.enum(["user", "assistant"]),
        content: zod.string(),
      }),
    )
    .describe("Full conversation history including the latest user message"),
  category: zod
    .enum(["all", "zakat", "iesco", "transport", "marriage", "emergency"])
    .optional()
    .describe("Optional category filter to focus the assistant's answer"),
});

export const SendChatMessageResponse = zod.object({
  reply: zod.string().describe("Assistant's reply text"),
  suggestions: zod
    .array(
      zod.object({
        label: zod.string(),
        prompt: zod.string(),
      }),
    )
    .describe("Optional follow-up suggestions"),
});
